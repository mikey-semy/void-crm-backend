# Alembic Migrations Guide

## 🚀 Быстрый старт

### Создание новой миграции

```bash
# Автоматическая генерация на основе изменений в моделях
alembic revision --autogenerate -m "Add initial tables"

# Ручное создание пустой миграции
alembic revision -m "Custom migration name"
```

### Применение миграций

```bash
# Применить все миграции
alembic upgrade head

# Применить следующую миграцию
alembic upgrade +1

# Откатить на одну миграцию назад
alembic downgrade -1

# Откатить все миграции
alembic downgrade base
```

### Просмотр информации

```bash
# Текущая версия БД
alembic current

# История миграций
alembic history

# Показать SQL без выполнения
alembic upgrade head --sql
```

## 🐳 Docker окружения

### Development (docker-compose.dev.yml)
Локальная разработка с полным стеком:
```bash
docker-compose -f docker-compose.dev.yml up -d
```
Миграции применяются автоматически при старте backend контейнера.

### Test (docker-compose.test.yml)
Тестовое окружение:
```bash
docker-compose -f docker-compose.test.yml up -d
```

### Production (docker-compose.traefik.yml)
Клиентский сервер с Traefik:
```bash
docker-compose -f docker-compose.traefik.yml up -d
```

### Dokploy (docker-compose.yml)
Сервер с внешней БД:
```bash
docker-compose up -d
```

## 📋 Структура

```
src/core/migrations/
├── versions/           # Файлы миграций (автогенерируемые)
├── env.py             # Конфигурация Alembic (async)
├── script.py.mako     # Шаблон для новых миграций
└── README             # Этот файл
```

## ⚙️ Конфигурация

### alembic.ini
- `script_location = src/core/migrations` - путь к миграциям
- URL БД берется из `settings.database_url` (env.py)

### env.py
- Использует async migrations (`run_async_migrations`)
- Автоматически импортирует `BaseModel.metadata`
- URL формируется из переменных окружения через `settings`

## 🔧 Ручной запуск миграций в Docker

```bash
# Войти в контейнер
docker exec -it profitool-backend sh

# Внутри контейнера
python -m alembic upgrade head
```

## 🐛 Troubleshooting

### "Can't locate revision identified by..."
Удалите `alembic_version` из БД и примените миграции заново:
```sql
DELETE FROM alembic_version;
```

### "Target database is not up to date"
Убедитесь что БД пустая или примените все миграции:
```bash
alembic upgrade head
```

### Конфликт миграций
Если несколько веток имеют миграции:
```bash
# Объединить головы
alembic merge heads -m "Merge migrations"
```

## 📝 Примечания

- Миграции применяются **автоматически** в `docker-entrypoint.sh`
- Для production рекомендуется ручное применение миграций перед deploy
- Всегда проверяйте сгенерированные миграции перед применением
- Делайте резервные копии БД перед применением миграций в production
